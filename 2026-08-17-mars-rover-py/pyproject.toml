[project]
name = "rover"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Michael Milewski", email = "saramic@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
rover = "rover:main"

[build-system]
requires = ["uv_build>=0.11.26,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "flake8>=7.3.0",
    "pytest>=9.1.1",
]
